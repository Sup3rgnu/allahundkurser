<div class="nav-links">
  <%= link_to "Allahundkurser", root_path, :class => 'brand' %>
  <%= link_to "Admin", courses_path, :class => 'sub_nav' %>
  <%= link_to "Organisationer", orgs_path, :class => 'sub_nav' %>
  <%= link_to "Kurser", courses_path, :class => 'sub_nav' %>

  <% if user_signed_in? %>
    <%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>
  <% else %>
    <%= link_to('Login', new_user_session_path)  %>
  <% end %>
  <% if user_signed_in? %>
    Inloggad som <%= current_user.email %>
    <%= link_to('Edit account', edit_user_registration_path) %>
  <% else %>
    <%= link_to('Sign up', new_user_registration_path)  %>
  <% end %>

</div>
<div id="search-wrapper">
  <form action="<%= courses_path %>" method="get" class="form-search">
      <div class="input-prepend">

        <%= submit_tag '', :name => '', :value => 'Sök', :class => 'btn btn-success' %>
        class="search-query" placeholder="Search"
        <%= text_field_tag :search, params[:search], :placeholder => 'Sök på stad, landskap, kurstyp eller namn', :class => 'input-xlarge search-query' %>      

      </div>
  </form>
</div>

