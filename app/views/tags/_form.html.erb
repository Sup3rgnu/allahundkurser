<%= form_for(@tag) do |f| %>

  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field span4">
    <%= f.label :name, 'Namn på tag/kategori' %>
    <%= f.text_field :name %>
  </div>

  <div class="spacer-small span4"></div>

  <div class="actions span4">
    <%= f.submit :value => 'Skapa tag', :class => 'btn btn-primary' %>
    <%= link_to 'Tillbaka', tags_path(), :class => 'btn' %>
  </div>

<% end %>
